codecov:
  branch: develop

  status:
    project:
      default: off
      frontend:
        target: 70%
        flags: frontend
      backend:
        target: 70%
        flags: backend

flags:
  backend:
    paths:
      - backend/
  frontend:
    paths:
      - frontend/
