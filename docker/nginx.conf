server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        try_files $uri?$args $uri/ $uri.html?$args /index.html?$args;
    }
    location = /index.html {
        root /usr/share/nginx/html;
        expires 30s;
    }
}
